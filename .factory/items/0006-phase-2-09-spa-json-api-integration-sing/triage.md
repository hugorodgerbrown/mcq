# Triage — Phase 2 · 09 · SPA <-> JSON API integration + single-origin Render deploy

**Build call:** BUILD (owner-approved in full; no candidate rejected).
**Priority:** 6 (build-order rank).
**Kind:** mixed.

## Scope
Mirrors Linear ticket **DSC-9** (team Q&A). Extracted from the Phase 2
PRD (source: docs/factory/prd/dsc1-phase-2.md #In-scope; #Open-questions Deploy shape).

## Dependencies
Blocked by 0001/0002/0003. Blocks 0007, 0008 (need API + deploy).

## Provenance
Not council-triaged: scope was approved directly by the owner on 2026-07-09.
Answer-confidence scale is out of scope (owner: the Meat-track `conf` field was
an in-flight temporary data point — ignore it). Free-text `Source` provenance
(item 0010 / DSC-10) is unaffected.
