# Triage — Phase 2 · 04 · CSV ingestion pipeline (two-phase import, merge on Code)

**Build call:** BUILD (owner-approved in full; no candidate rejected).
**Priority:** 4 (build-order rank).
**Kind:** mixed.

## Scope
Mirrors Linear ticket **DSC-4** (team Q&A). Extracted from the Phase 2
PRD (source: docs/factory/prd/dsc1-phase-2.md #Ingestion; #CSV-contract).

## Dependencies
Blocked by 0002 (data model). Pairs with 0005 (validation). Blocks 0007, 0008, 0010.

## Provenance
Not council-triaged: scope was approved directly by the owner on 2026-07-09.
Answer-confidence scale is out of scope (owner: the Meat-track `conf` field was
an in-flight temporary data point — ignore it). Free-text `Source` provenance
(item 0010 / DSC-10) is unaffected.
