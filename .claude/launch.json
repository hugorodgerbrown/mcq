{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "web",
      "runtimeExecutable": "sh",
      "runtimeArgs": [
        "-c",
        "npm run build && cd server && uv run python manage.py migrate --noinput && uv run python manage.py runserver 0.0.0.0:$PORT"
      ],
      "autoPort": true,
      "port": 8000
    }
  ]
}
